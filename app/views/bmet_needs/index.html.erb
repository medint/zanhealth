<h1><%= @language['Listing'] @language['Need'].pluralize %></h1>

<table>
  <thead>
    <tr>
      <th><%= @language['Name']%></th>
      <th><%= @language['Location']%></th>
      <th><%= @language['Model']%></th>
      <th><%= @language['Quantity']%></th>
      <th><%= @language['Urgency']%></th>
      <th><%= @language['Reason']%></th>
      <th><%= @language['Remarks']%></th>
      <th><%= @language['Stage']%></th>
      <th><%= @language['Date requested']%></th>
      <th><%= @language['User']%></th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @needs.each do |need| %>
      <tr>
        <td><%= need.name %></td>
        <td><%= need.department_id %></td>
        <td><%= need.bmet_model_id %></td>
        <td><%= need.quantity %></td>
        <td><%= need.urgency %></td>
        <td><%= need.reason %></td>
        <td><%= need.remarks %></td>
        <td><%= need.stage %></td>
        <td><%= need.date_requested %></td>
        <td><%= need.user_id %></td>
        <td><%= link_to @language['Show'], bmet_need %></td>
        <td><%= link_to @language['Edit'], edit_bmet_need_path(need) %></td>
        <td><%= link_to @language['Destroy'], need, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to @language['New'] @language['Need'], new_bmet_need_path %>
