<% if flash[:notice] %>
	<% flash.each do |key, value| %>
        <div id="flash-item-notice"><%= value %></div>
    <% end %>
<% end %>

<%= render :partial => 'interaction_bar', :layout => 'layouts/topbar' %>

<%= render :partial =>'empty_summary', :layout =>'layouts/col2' %>

<div class="vertical-divider"></div>

<%= render :partial =>'object_summary', :layout =>'layouts/col3' %>