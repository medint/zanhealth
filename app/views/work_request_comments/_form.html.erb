<%= form_for(@work_request_comment) do |f| %>
  <% if @work_request_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_request_comment.errors.count, "error") %> prohibited this work_request_comment from being saved:</h2>

      <ul>
      <% @work_request_comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :datetime_stamp %><br>
    <%= f.datetime_select :datetime_stamp %>
  </div>
  <div class="field">
    <%= f.label :comment_text %><br>
    <%= f.text_area :comment_text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
