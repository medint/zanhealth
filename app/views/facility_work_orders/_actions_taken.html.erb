  <%= form_for(@facility_work_order) do |f| %>
    <% if @facility_work_order.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@facility_work_order.errors.count, "error") %> prohibited this facility_work_order from being saved:</h2>
            <ul> 
              <% @facility_work_order.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
              <% end %>
            </ul>
        </div>x
    <% end %>

    <div class="extra-details-box">   
        <div class="field-type"><%= t(:cause_description).titlecase %></div>
        <div class="field-info"><%= f.text_area :cause_description %></div>
    </div>

    <div class="extra-details-box">   
        <div class="field-type"><%= t(:action_taken).titlecase %></div>
        <div class="field-info"><%= f.text_area :action_taken %></div>
    </div>

    <div class="extra-details-box">   
        <div class="field-type"><%= t(:prevention_taken).titlecase %></div>
        <div class="field-info"><%= f.text_area :prevention_taken %></div>
    </div>

    <div class="extra-details-box">
      <% if @facility_work_order.wr_origin %>
          <a style="color:white" href="<%= facility_work_requests_url + '/all/' + @facility_work_order.wr_origin.to_s %>">
            View original work request
          </a>
      <% elsif @facility_work_order.pm_origin %>
          <a style="color:white" href="<%= facility_preventative_maintenances_url + '/all/' + @facility_work_order.pm_origin.to_s %>">
            View original preventative maintenance
          </a>
      <% else %>
      <% end %>
    </div>
    &nbsp
    <div class="actions">
      <%= f.submit t(:save_changes).titlecase%>
    </div>
  <%end%>
